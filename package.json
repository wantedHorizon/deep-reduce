{
  "name": "deep-reduce",
  "version": "1.0.0",
  "description": "Deep reducer function with schema ",
  "main": "lib/index.js",
  "scripts": {
    "build": "./node_modules/.bin/babel src --out-dir lib",
    "prepare": "npm run build",
    "start": "npm run build &&node lib/index",
    "test1": "npm run build && node lib/test.js",
    "test": "mocha --require @babel/register test/*.test.js"
  },
  "author": "wantedHorizon",
  "license": "ISC",
  "devDependencies": {
    "@babel/cli": "^7.12.10",
    "@babel/core": "^7.12.10",
    "@babel/preset-env": "^7.12.11",
    "@babel/register": "^7.12.10"
  },
  "dependencies": {
    "deepcopy": "^2.1.0"
  }
}
